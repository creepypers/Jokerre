import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  modalHeader: {
    alignItems: 'center',
    marginBottom: 16,
  },
  modalIcon: {
    fontSize: 32,
    marginBottom: 8,
  },
});
